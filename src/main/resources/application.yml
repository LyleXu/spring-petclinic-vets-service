spring:
  profiles:
    active: production
  cache:
    cache-names: vets
  cloud:
    config:
      uri: http://config-server.default.learning.petclinic.com
  application:
    name: vets-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server.default.learning.petclinic.com/eureka/
