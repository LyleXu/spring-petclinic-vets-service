spring:
  profiles:
    active: production
  cache:
    cache-names: vets
  application:
    name: vets-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server.service-registry/eureka
