spring:
  profiles:
    active: production
  cache:
    cache-names: vets
  application:
    name: vets-service
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://eureka-server.service-registry/eureka

