spring:
  profiles:
    active: production
  cache:
    cache-names: vets
  cloud:
    config:
      uri: http://config-server-00001-private
  application:
    name: vets-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server-00001-private/eureka
