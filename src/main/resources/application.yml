spring:
  profiles:
    active: production
  cache:
    cache-names: vets
  cloud:
    config:
      uri: http://config-server.default.learning.petclinic.com:8888
  application:
    name: vets-service

